/*
SELECT
*/
--Obtine todos los datos de la tabla "users"

SELECT * FROM users;

-- Obtiene todos los nombre de la tabla "users"

SELECT name FROM users;

-- Obtiene todos los identificadores y nombre de la tabla "users"

SELECT user_id, name FROM users;